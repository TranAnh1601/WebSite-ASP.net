@*@using PagedList;
@using PagedList.Mvc;

@model WebSiteBanHang.Models.HomeModel*@

@model IEnumerable<WebSiteBanHang.Context.Product>
@{
    ViewBag.Title = "Search";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



<section class="padding-bottom-sm">

    <header class="section-heading heading-line">
        <h4 class="title-section text-uppercase">SẢN PHẨM TÌM KIẾM</h4>
    </header>
    <h4>Kết quả tìm kiếm của <p class="text-danger">@Model.Count()</p> sản phẩm</h4>
    <div class="row row-sm">

        @foreach (var item in Model)
        {
            var urlDetail = "san-pham/" + "/" + item.Slug + "/" + item.Id;
            <div class="col-xl-2 col-lg-3 col-md-4 col-6">
                <div class="card card-sm card-product-grid">
                    <a href="@urlDetail" class="img-wrap"> <img src="~/Content/images/items/@item.Avatar"> </a>
                    <figcaption class="info-wrap">
                        <a href="@urlDetail" class="title">@item.Name</a>
                        <div class="price mt-1">@item.Price</div> <!-- price-wrap.// -->
                    </figcaption>
                </div>
            </div> <!-- col.// -->
        }
    @*<div class="container">

        <div class="row">
            <div class="text-center">
                Trang @(Model.lstProduct.PageCount<Model.lstProduct.PageNumber? 0: Model.lstProduct.PageNumber) of @Model.lstProduct.PageCount
                <nav>
                    @Html.PagedListPager(Model.lstProduct, page => Url.Action("ProductCategory",
                           new { page, currentFilter = ViewBag.curentFilter }))
                </nav>
            </div>
        </div>

    </div>*@
   



    </div> <!-- row.// -->
</section>


